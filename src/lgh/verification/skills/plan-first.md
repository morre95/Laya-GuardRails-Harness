# Plan first

This change touches a large file set.

1. Summarize the intended file set.
2. Split the change into reviewable steps.
3. Confirm the user goal still matches.
4. Avoid unrelated refactors.

Then retry the original action so the guardrail can re-evaluate it.
